org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Build cache nằm trong Gradle User Home, đúng thư mục setup-gradle lưu lại giữa
# các lần chạy CI — bật lên thì lần build sau tái dùng được output đã biên dịch
# thay vì làm lại từ đầu. Parallel tận dụng nhiều nhân của runner.
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
