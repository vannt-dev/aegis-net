# AegisNet App Launcher Icon — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thay icon Flutter mặc định trên Android, iOS và web bằng icon AegisNet (khiên + node mạng, neon cyan trên nền đen).

**Architecture:** Nguồn thiết kế là 4 file SVG viết tay. Một script Node điều khiển Chrome headless render chúng thành PNG 1024×1024. Package `flutter_launcher_icons` fan-out các PNG đó ra toàn bộ thư mục của từng nền tảng. Kiểm chứng bằng một bộ decoder PNG thuần Node (dùng `zlib` có sẵn) — không thêm dependency runtime nào.

**Tech Stack:** SVG · Node.js 24 (`node:test`, `node:zlib`, không thêm npm package) · Chrome headless · `flutter_launcher_icons` 0.14.4 (dev dependency)

**Spec:** `docs/superpowers/specs/2026-07-30-app-icon-design.md`

## Global Constraints

- Màu accent: `#18FFFF` (khớp `Colors.cyanAccent` trong `lib/src/providers/theme_provider.dart`).
- Nền: radial gradient `#111B22` (giữa) → `#0A0F14` (rìa).
- Canvas gốc: 1024×1024. Mọi toạ độ trong plan này tính trên hệ đó.
- **Safe zone:** mọi pixel không trong suốt của khiên + mạch phải nằm trong bán kính **312px** tính từ `(512, 512)`. Glow được miễn trừ.
- Hình khiên: path cao 542px (`y` 222→764), rộng nhất 396px (`x` 314→710), nét viền 34px, `stroke-linejoin="round"`.
- **Không** thêm bất kỳ npm package nào. Chỉ dùng thư viện chuẩn của Node.
- **Không** khai báo `assets/branding/` vào `flutter: assets:` trong `pubspec.yaml` — đây là input lúc build, không phải asset runtime.
- Thông báo lỗi trong script viết tiếng Việt **không dấu** (script chạy trong terminal Windows, dấu tiếng Việt dễ hỏng encoding).
- Chỉ chạy Task 4 sau khi người dùng duyệt xong checkpoint ở Task 3.

## File Structure

| File | Trách nhiệm |
|---|---|
| `assets/branding/pnginfo.mjs` | Đọc PNG thuần Node: metadata + giải mã ra RGBA. Không biết gì về icon. |
| `assets/branding/pnginfo.test.mjs` | Test cho `pnginfo.mjs`, tự sinh PNG mẫu để kiểm. |
| `assets/branding/icon_background.svg` | Layer nền gradient. |
| `assets/branding/icon_foreground.svg` | Layer khiên + mạch + glow, nền trong suốt. |
| `assets/branding/icon_full.svg` | Nền + khiên + mạch, đục hoàn toàn. |
| `assets/branding/icon_monochrome.svg` | Silhouette một màu trắng, mạch khoét rỗng. |
| `assets/branding/render.mjs` | Dò Chrome, bọc SVG vào HTML, render ra `generated/*.png`. |
| `assets/branding/render.test.mjs` | Kiểm 4 PNG sinh ra: kích thước, alpha, safe zone. |
| `assets/branding/preview.html` | Trang xem thử cho người duyệt. Không tham gia build. |
| `assets/branding/verify.mjs` | Kiểm asset đã fan-out ra Android/iOS/web. |
| `pubspec.yaml` | Thêm dev dep + khối cấu hình `flutter_launcher_icons`. |
| `web/manifest.json`, `web/index.html` | Sửa branding còn sót của Flutter. |

`pnginfo.mjs` tách riêng khỏi cả `render.test.mjs` lẫn `verify.mjs` vì cả hai đều cần đọc PNG, và bản thân nó là thứ duy nhất trong nhóm này test được độc lập.

---

### Task 1: Bộ đọc PNG thuần Node

**Files:**
- Create: `assets/branding/pnginfo.mjs`
- Test: `assets/branding/pnginfo.test.mjs`

**Interfaces:**
- Consumes: không có (task đầu tiên).
- Produces:
  - `pngMeta(filePath: string) -> { width: number, height: number, bitDepth: number, colorType: number, interlace: number }`
  - `decodeRgba(filePath: string) -> { width: number, height: number, data: Uint8Array }` — `data` dài `width*height*4`, thứ tự R,G,B,A.
  - `hasAnyTransparent(img: {data: Uint8Array}) -> boolean`
  - `alphaAt(img: {width:number,data:Uint8Array}, x: number, y: number) -> number`

- [ ] **Step 1: Viết test thất bại**

Tạo `assets/branding/pnginfo.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { deflateSync } from 'node:zlib';
import { writeFileSync, mkdtempSync } from 'node:fs';
import { join } from 'node:path';
import { tmpdir } from 'node:os';

import { pngMeta, decodeRgba, hasAnyTransparent, alphaAt } from './pnginfo.mjs';

const SIG = Buffer.from([0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a]);

const CRC_TABLE = (() => {
  const t = new Int32Array(256);
  for (let n = 0; n < 256; n++) {
    let c = n;
    for (let k = 0; k < 8; k++) c = c & 1 ? 0xedb88320 ^ (c >>> 1) : c >>> 1;
    t[n] = c;
  }
  return t;
})();

function crc32(buf) {
  let c = 0xffffffff;
  for (let i = 0; i < buf.length; i++) c = CRC_TABLE[(c ^ buf[i]) & 0xff] ^ (c >>> 8);
  return (c ^ 0xffffffff) >>> 0;
}

function chunk(type, data) {
  const len = Buffer.alloc(4);
  len.writeUInt32BE(data.length);
  const td = Buffer.concat([Buffer.from(type, 'ascii'), data]);
  const crc = Buffer.alloc(4);
  crc.writeUInt32BE(crc32(td));
  return Buffer.concat([len, td, crc]);
}

// rawScanlines: buffer da bao gom byte filter dau moi dong.
function assemblePng(width, height, rawScanlines) {
  const ihdr = Buffer.alloc(13);
  ihdr.writeUInt32BE(width, 0);
  ihdr.writeUInt32BE(height, 4);
  ihdr[8] = 8;  // bit depth
  ihdr[9] = 6;  // color type RGBA
  return Buffer.concat([
    SIG,
    chunk('IHDR', ihdr),
    chunk('IDAT', deflateSync(rawScanlines)),
    chunk('IEND', Buffer.alloc(0)),
  ]);
}

// Moi dong dung filter 0 (None).
function makePng(width, height, pixelFn) {
  const raw = Buffer.alloc(height * (1 + width * 4));
  let p = 0;
  for (let y = 0; y < height; y++) {
    raw[p++] = 0;
    for (let x = 0; x < width; x++) {
      const [r, g, b, a] = pixelFn(x, y);
      raw[p++] = r; raw[p++] = g; raw[p++] = b; raw[p++] = a;
    }
  }
  return assemblePng(width, height, raw);
}

const DIR = mkdtempSync(join(tmpdir(), 'pnginfo-'));
function writeTemp(name, buf) {
  const p = join(DIR, name);
  writeFileSync(p, buf);
  return p;
}

test('pngMeta doc dung kich thuoc va color type', () => {
  const p = writeTemp('meta.png', makePng(7, 3, () => [1, 2, 3, 255]));
  const m = pngMeta(p);
  assert.equal(m.width, 7);
  assert.equal(m.height, 3);
  assert.equal(m.bitDepth, 8);
  assert.equal(m.colorType, 6);
  assert.equal(m.interlace, 0);
});

test('decodeRgba tra ve dung gia tri pixel', () => {
  const p = writeTemp('pixels.png', makePng(2, 2, (x, y) => [x * 10, y * 20, 30, x === y ? 255 : 0]));
  const img = decodeRgba(p);
  assert.equal(img.width, 2);
  assert.equal(img.height, 2);
  assert.deepEqual([...img.data.slice(0, 4)], [0, 0, 30, 255]);   // (0,0)
  assert.deepEqual([...img.data.slice(4, 8)], [10, 0, 30, 0]);    // (1,0)
  assert.equal(alphaAt(img, 1, 1), 255);
  assert.equal(alphaAt(img, 0, 1), 0);
});

test('decodeRgba go dung filter type 2 (Up)', () => {
  // Dong 0: filter None, pixel (10,20,30,40).
  // Dong 1: filter Up, delta (5,5,5,5) -> ket qua phai la (15,25,35,45).
  const raw = Buffer.from([
    0, 10, 20, 30, 40,
    2, 5, 5, 5, 5,
  ]);
  const p = writeTemp('up.png', assemblePng(1, 2, raw));
  const img = decodeRgba(p);
  assert.deepEqual([...img.data.slice(0, 4)], [10, 20, 30, 40]);
  assert.deepEqual([...img.data.slice(4, 8)], [15, 25, 35, 45]);
});

test('hasAnyTransparent phan biet dung anh duc va anh co alpha', () => {
  const opaque = writeTemp('opaque.png', makePng(3, 3, () => [0, 0, 0, 255]));
  const holed = writeTemp('holed.png', makePng(3, 3, (x) => [0, 0, 0, x === 1 ? 0 : 255]));
  assert.equal(hasAnyTransparent(decodeRgba(opaque)), false);
  assert.equal(hasAnyTransparent(decodeRgba(holed)), true);
});

test('decodeRgba bao loi ro rang voi file khong phai PNG', () => {
  const p = writeTemp('bad.bin', Buffer.from('day khong phai png'));
  assert.throws(() => decodeRgba(p), /khong phai file PNG/i);
});
```

- [ ] **Step 2: Chạy test để xác nhận nó thất bại**

Run: `node --test "assets/branding/*.test.mjs"pnginfo.test.mjs`
Expected: FAIL — `Cannot find module ... pnginfo.mjs`

- [ ] **Step 3: Viết `pnginfo.mjs`**

Tạo `assets/branding/pnginfo.mjs`:

```js
// Doc PNG bang thu vien chuan cua Node. Chi ho tro dung tap con ma Chrome
// headless xuat ra: bit depth 8, khong interlace, color type 2 (RGB) hoac 6 (RGBA).
import { readFileSync } from 'node:fs';
import { inflateSync } from 'node:zlib';

const SIG = Buffer.from([0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a]);

function readChunks(buf, filePath) {
  if (buf.length < 8 || !buf.subarray(0, 8).equals(SIG)) {
    throw new Error(`Khong phai file PNG hop le: ${filePath}`);
  }
  const chunks = [];
  let off = 8;
  while (off + 8 <= buf.length) {
    const len = buf.readUInt32BE(off);
    const type = buf.toString('ascii', off + 4, off + 8);
    chunks.push({ type, data: buf.subarray(off + 8, off + 8 + len) });
    off += 12 + len;
    if (type === 'IEND') break;
  }
  return chunks;
}

export function pngMeta(filePath) {
  const chunks = readChunks(readFileSync(filePath), filePath);
  const ihdr = chunks.find((c) => c.type === 'IHDR');
  if (!ihdr) throw new Error(`Thieu chunk IHDR: ${filePath}`);
  return {
    width: ihdr.data.readUInt32BE(0),
    height: ihdr.data.readUInt32BE(4),
    bitDepth: ihdr.data[8],
    colorType: ihdr.data[9],
    interlace: ihdr.data[12],
  };
}

function paeth(a, b, c) {
  const p = a + b - c;
  const pa = Math.abs(p - a);
  const pb = Math.abs(p - b);
  const pc = Math.abs(p - c);
  if (pa <= pb && pa <= pc) return a;
  return pb <= pc ? b : c;
}

export function decodeRgba(filePath) {
  const buf = readFileSync(filePath);
  const chunks = readChunks(buf, filePath);
  const meta = pngMeta(filePath);

  if (meta.bitDepth !== 8) {
    throw new Error(`Chi ho tro bit depth 8, gap ${meta.bitDepth}: ${filePath}`);
  }
  if (meta.interlace !== 0) {
    throw new Error(`Khong ho tro PNG interlaced: ${filePath}`);
  }
  if (meta.colorType !== 2 && meta.colorType !== 6) {
    throw new Error(`Chi ho tro color type 2 hoac 6, gap ${meta.colorType}: ${filePath}`);
  }

  const bpp = meta.colorType === 6 ? 4 : 3;
  const idat = chunks.filter((c) => c.type === 'IDAT').map((c) => c.data);
  if (idat.length === 0) throw new Error(`Thieu chunk IDAT: ${filePath}`);
  const raw = inflateSync(Buffer.concat(idat));

  const stride = meta.width * bpp;
  const out = new Uint8Array(meta.width * meta.height * 4);
  let prev = Buffer.alloc(stride);
  let pos = 0;

  for (let y = 0; y < meta.height; y++) {
    const ft = raw[pos++];
    const line = Buffer.from(raw.subarray(pos, pos + stride));
    pos += stride;

    for (let i = 0; i < stride; i++) {
      const a = i >= bpp ? line[i - bpp] : 0;
      const b = prev[i];
      const c = i >= bpp ? prev[i - bpp] : 0;
      let v = line[i];
      if (ft === 1) v += a;
      else if (ft === 2) v += b;
      else if (ft === 3) v += (a + b) >> 1;
      else if (ft === 4) v += paeth(a, b, c);
      else if (ft !== 0) throw new Error(`Filter type ${ft} khong hop le tai dong ${y}: ${filePath}`);
      line[i] = v & 0xff;
    }

    for (let x = 0; x < meta.width; x++) {
      const s = x * bpp;
      const d = (y * meta.width + x) * 4;
      out[d] = line[s];
      out[d + 1] = line[s + 1];
      out[d + 2] = line[s + 2];
      out[d + 3] = bpp === 4 ? line[s + 3] : 255;
    }
    prev = line;
  }

  return { width: meta.width, height: meta.height, data: out };
}

export function alphaAt(img, x, y) {
  return img.data[(y * img.width + x) * 4 + 3];
}

export function hasAnyTransparent(img) {
  for (let i = 3; i < img.data.length; i += 4) {
    if (img.data[i] !== 255) return true;
  }
  return false;
}
```

- [ ] **Step 4: Chạy test để xác nhận nó pass**

Run: `node --test "assets/branding/*.test.mjs"pnginfo.test.mjs`
Expected: PASS — `# pass 5`, `# fail 0`

- [ ] **Step 5: Commit**

```bash
git add assets/branding/pnginfo.mjs assets/branding/pnginfo.test.mjs
git commit -m "feat: add pure-Node PNG reader for icon asset verification"
```

---

### Task 2: SVG nguồn và pipeline render

**Files:**
- Create: `assets/branding/icon_background.svg`
- Create: `assets/branding/icon_foreground.svg`
- Create: `assets/branding/icon_full.svg`
- Create: `assets/branding/icon_monochrome.svg`
- Create: `assets/branding/render.mjs`
- Modify: `.gitignore` (thêm `assets/branding/.render-tmp/`)
- Test: `assets/branding/render.test.mjs`

**Interfaces:**
- Consumes: `pngMeta`, `decodeRgba`, `alphaAt`, `hasAnyTransparent` từ `./pnginfo.mjs` (Task 1).
- Produces:
  - `assets/branding/generated/icon_full.png` — 1024×1024, đục hoàn toàn
  - `assets/branding/generated/icon_foreground.png` — 1024×1024, nền trong suốt
  - `assets/branding/generated/icon_background.png` — 1024×1024, đục hoàn toàn
  - `assets/branding/generated/icon_monochrome.png` — 1024×1024, nền trong suốt
  - `render.mjs` xuất `LAYERS: string[]` và `renderAll() -> void`

- [ ] **Step 1: Viết test thất bại**

Tạo `assets/branding/render.test.mjs`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { existsSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

import { pngMeta, decodeRgba, alphaAt, hasAnyTransparent } from './pnginfo.mjs';

const HERE = dirname(fileURLToPath(import.meta.url));
const GEN = join(HERE, 'generated');

const SIZE = 1024;
const CENTER = 512;
const SAFE_RADIUS = 312;  // 66/108 cua canvas, xem spec 3.4

const TRANSPARENT_BG = ['icon_foreground', 'icon_monochrome'];
const OPAQUE = ['icon_full', 'icon_background'];
const ALL = [...OPAQUE, ...TRANSPARENT_BG];

test('ca 4 layer deu ton tai o dung 1024x1024', () => {
  for (const name of ALL) {
    const p = join(GEN, `${name}.png`);
    assert.ok(existsSync(p), `Thieu file ${name}.png — da chay "node assets/branding/render.mjs" chua?`);
    const m = pngMeta(p);
    assert.equal(m.width, SIZE, `${name}.png sai chieu rong`);
    assert.equal(m.height, SIZE, `${name}.png sai chieu cao`);
  }
});

test('layer foreground va monochrome co goc trong suot', () => {
  for (const name of TRANSPARENT_BG) {
    const img = decodeRgba(join(GEN, `${name}.png`));
    for (const [x, y] of [[0, 0], [SIZE - 1, 0], [0, SIZE - 1], [SIZE - 1, SIZE - 1]]) {
      assert.equal(
        alphaAt(img, x, y), 0,
        `${name}.png co goc (${x},${y}) khong trong suot — Chrome da chen nen trang?`,
      );
    }
  }
});

test('layer full va background duc hoan toan', () => {
  for (const name of OPAQUE) {
    const img = decodeRgba(join(GEN, `${name}.png`));
    assert.equal(hasAnyTransparent(img), false, `${name}.png con pixel trong suot`);
  }
});

test('monochrome nam gon trong safe zone 312px', () => {
  const img = decodeRgba(join(GEN, 'icon_monochrome.png'));
  const limitSq = SAFE_RADIUS * SAFE_RADIUS;
  let worstSq = 0;
  let worstAt = null;

  for (let y = 0; y < img.height; y++) {
    for (let x = 0; x < img.width; x++) {
      if (alphaAt(img, x, y) === 0) continue;
      const dx = x - CENTER;
      const dy = y - CENTER;
      const dSq = dx * dx + dy * dy;
      if (dSq > worstSq) { worstSq = dSq; worstAt = [x, y]; }
    }
  }

  assert.ok(
    worstSq <= limitSq,
    `Pixel xa nhat (${worstAt}) cach tam ${Math.sqrt(worstSq).toFixed(1)}px, vuot safe zone ${SAFE_RADIUS}px`,
  );
});

test('monochrome la anh don sac trang', () => {
  const img = decodeRgba(join(GEN, 'icon_monochrome.png'));
  for (let i = 0; i < img.data.length; i += 4) {
    if (img.data[i + 3] === 0) continue;
    assert.equal(img.data[i], 255, 'monochrome phai la mau trang thuan');
    assert.equal(img.data[i + 1], 255, 'monochrome phai la mau trang thuan');
    assert.equal(img.data[i + 2], 255, 'monochrome phai la mau trang thuan');
  }
});
```

- [ ] **Step 2: Chạy test để xác nhận nó thất bại**

Run: `node --test "assets/branding/*.test.mjs"render.test.mjs`
Expected: FAIL — `Thieu file icon_full.png — da chay "node assets/branding/render.mjs" chua?`

- [ ] **Step 3: Viết 4 file SVG**

Tạo `assets/branding/icon_background.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1024" viewBox="0 0 1024 1024">
  <defs>
    <radialGradient id="bg" cx="50%" cy="45%" r="78%">
      <stop offset="0%" stop-color="#111B22"/>
      <stop offset="100%" stop-color="#0A0F14"/>
    </radialGradient>
  </defs>
  <rect width="1024" height="1024" fill="url(#bg)"/>
</svg>
```

Tạo `assets/branding/icon_foreground.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1024" viewBox="0 0 1024 1024">
  <defs>
    <filter id="glow" x="-30%" y="-30%" width="160%" height="160%">
      <feGaussianBlur stdDeviation="18" result="blur"/>
      <feComponentTransfer in="blur" result="soft">
        <feFuncA type="linear" slope="0.45"/>
      </feComponentTransfer>
      <feMerge>
        <feMergeNode in="soft"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
    <filter id="halo" x="-100%" y="-100%" width="300%" height="300%">
      <feGaussianBlur stdDeviation="26"/>
    </filter>
  </defs>

  <g filter="url(#glow)">
    <path d="M 512 222 Q 636 222 690 254 Q 710 266 710 300 L 710 468 Q 710 604 598 700 Q 552 740 512 764 Q 472 740 426 700 Q 314 604 314 468 L 314 300 Q 314 266 334 254 Q 388 222 512 222 Z"
          fill="#18FFFF" fill-opacity="0.07"
          stroke="#18FFFF" stroke-width="34" stroke-linejoin="round"/>

    <g stroke="#18FFFF" stroke-opacity="0.55" stroke-width="20" stroke-linecap="round">
      <line x1="512" y1="470" x2="512" y2="352"/>
      <line x1="512" y1="470" x2="410" y2="529"/>
      <line x1="512" y1="470" x2="614" y2="529"/>
    </g>

    <circle cx="512" cy="470" r="76" fill="#18FFFF" opacity="0.28" filter="url(#halo)"/>
    <circle cx="512" cy="352" r="30" fill="#18FFFF"/>
    <circle cx="410" cy="529" r="30" fill="#18FFFF"/>
    <circle cx="614" cy="529" r="30" fill="#18FFFF"/>
    <circle cx="512" cy="470" r="46" fill="#18FFFF"/>
  </g>
</svg>
```

Tạo `assets/branding/icon_full.svg` — giống hệt foreground nhưng có thêm `<rect>` nền, và phần nền nằm **ngoài** group có filter để glow không làm bẩn nền:

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1024" viewBox="0 0 1024 1024">
  <defs>
    <radialGradient id="bg" cx="50%" cy="45%" r="78%">
      <stop offset="0%" stop-color="#111B22"/>
      <stop offset="100%" stop-color="#0A0F14"/>
    </radialGradient>
    <filter id="glow" x="-30%" y="-30%" width="160%" height="160%">
      <feGaussianBlur stdDeviation="18" result="blur"/>
      <feComponentTransfer in="blur" result="soft">
        <feFuncA type="linear" slope="0.45"/>
      </feComponentTransfer>
      <feMerge>
        <feMergeNode in="soft"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
    <filter id="halo" x="-100%" y="-100%" width="300%" height="300%">
      <feGaussianBlur stdDeviation="26"/>
    </filter>
  </defs>

  <rect width="1024" height="1024" fill="url(#bg)"/>

  <g filter="url(#glow)">
    <path d="M 512 222 Q 636 222 690 254 Q 710 266 710 300 L 710 468 Q 710 604 598 700 Q 552 740 512 764 Q 472 740 426 700 Q 314 604 314 468 L 314 300 Q 314 266 334 254 Q 388 222 512 222 Z"
          fill="#18FFFF" fill-opacity="0.07"
          stroke="#18FFFF" stroke-width="34" stroke-linejoin="round"/>

    <g stroke="#18FFFF" stroke-opacity="0.55" stroke-width="20" stroke-linecap="round">
      <line x1="512" y1="470" x2="512" y2="352"/>
      <line x1="512" y1="470" x2="410" y2="529"/>
      <line x1="512" y1="470" x2="614" y2="529"/>
    </g>

    <circle cx="512" cy="470" r="76" fill="#18FFFF" opacity="0.28" filter="url(#halo)"/>
    <circle cx="512" cy="352" r="30" fill="#18FFFF"/>
    <circle cx="410" cy="529" r="30" fill="#18FFFF"/>
    <circle cx="614" cy="529" r="30" fill="#18FFFF"/>
    <circle cx="512" cy="470" r="46" fill="#18FFFF"/>
  </g>
</svg>
```

Tạo `assets/branding/icon_monochrome.svg` — khiên đặc, mạch khoét rỗng bằng mask, không glow:

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1024" viewBox="0 0 1024 1024">
  <defs>
    <mask id="knockout">
      <path d="M 512 222 Q 636 222 690 254 Q 710 266 710 300 L 710 468 Q 710 604 598 700 Q 552 740 512 764 Q 472 740 426 700 Q 314 604 314 468 L 314 300 Q 314 266 334 254 Q 388 222 512 222 Z"
            fill="#FFFFFF" stroke="#FFFFFF" stroke-width="34" stroke-linejoin="round"/>

      <g stroke="#000000" stroke-width="20" stroke-linecap="round">
        <line x1="512" y1="470" x2="512" y2="352"/>
        <line x1="512" y1="470" x2="410" y2="529"/>
        <line x1="512" y1="470" x2="614" y2="529"/>
      </g>
      <circle cx="512" cy="352" r="30" fill="#000000"/>
      <circle cx="410" cy="529" r="30" fill="#000000"/>
      <circle cx="614" cy="529" r="30" fill="#000000"/>
      <circle cx="512" cy="470" r="46" fill="#000000"/>
    </mask>
  </defs>

  <rect width="1024" height="1024" fill="#FFFFFF" mask="url(#knockout)"/>
</svg>
```

- [ ] **Step 4: Viết `render.mjs`**

Tạo `assets/branding/render.mjs`:

```js
// Render cac layer SVG thanh PNG 1024x1024 bang Chrome headless.
// Chay: node assets/branding/render.mjs
import { readFileSync, writeFileSync, mkdirSync, existsSync, rmSync } from 'node:fs';
import { execFileSync } from 'node:child_process';
import { join, dirname } from 'node:path';
import { fileURLToPath, pathToFileURL } from 'node:url';

const HERE = dirname(fileURLToPath(import.meta.url));
const OUT = join(HERE, 'generated');
const TMP = join(HERE, '.render-tmp');
const SIZE = 1024;

export const LAYERS = ['icon_full', 'icon_foreground', 'icon_background', 'icon_monochrome'];

function findChrome() {
  const candidates = [
    process.env.CHROME_PATH,
    'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe',
    'C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe',
    'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe',
    'C:\\Program Files\\Microsoft\\Edge\\Application\\msedge.exe',
  ].filter(Boolean);

  for (const c of candidates) {
    if (existsSync(c)) return c;
  }
  throw new Error(
    'Khong tim thay Chrome hoac Edge. Dat bien moi truong CHROME_PATH tro toi chrome.exe roi chay lai.',
  );
}

// Boc SVG vao HTML kich thuoc co dinh. Render thang file .svg se de bi Chrome
// tu can le / scale theo mac dinh cua trinh duyet.
function wrap(svgSource) {
  return `<!doctype html>
<meta charset="utf-8">
<style>
  html, body { margin: 0; padding: 0; background: transparent; }
  svg { display: block; width: ${SIZE}px; height: ${SIZE}px; }
</style>
${svgSource}`;
}

export function renderAll() {
  const chrome = findChrome();
  mkdirSync(OUT, { recursive: true });
  mkdirSync(TMP, { recursive: true });

  for (const name of LAYERS) {
    const svgPath = join(HERE, `${name}.svg`);
    if (!existsSync(svgPath)) throw new Error(`Thieu file nguon: ${svgPath}`);

    const htmlPath = join(TMP, `${name}.html`);
    writeFileSync(htmlPath, wrap(readFileSync(svgPath, 'utf8')), 'utf8');

    const outPath = join(OUT, `${name}.png`);
    execFileSync(chrome, [
      '--headless',
      '--disable-gpu',
      '--hide-scrollbars',
      `--user-data-dir=${join(TMP, 'profile')}`,
      `--window-size=${SIZE},${SIZE}`,
      '--force-device-scale-factor=1',
      '--default-background-color=00000000',
      `--screenshot=${outPath}`,
      pathToFileURL(htmlPath).href,
    ], { stdio: 'pipe' });

    if (!existsSync(outPath)) {
      throw new Error(`Chrome khong tao duoc ${outPath}. Thu dat CHROME_PATH sang trinh duyet khac.`);
    }
    console.log(`OK  ${name}.png`);
  }

  // Chrome tren Windows co the con giu lock tren user-data-dir mot luc sau khi
  // thoat, khien rmSync nem EBUSY. Don dep that bai khong phai la loi render.
  try {
    rmSync(TMP, { recursive: true, force: true });
  } catch {
    console.log(`(Khong xoa duoc thu muc tam ${TMP} — bo qua, da duoc gitignore.)`);
  }
}

renderAll();
```

- [ ] **Step 5: Bỏ qua thư mục tạm trong git**

Thêm dòng sau vào cuối `.gitignore` (tạo file nếu chưa có):

```
assets/branding/.render-tmp/
```

Thư mục `assets/branding/generated/` **được commit** — để build lại được mà không cần Chrome.

- [ ] **Step 6: Chạy render**

Run: `node assets/branding/render.mjs`
Expected: in ra 4 dòng `OK  <tên>.png`, không có lỗi.

- [ ] **Step 7: Chạy test để xác nhận nó pass**

Run: `node --test "assets/branding/*.test.mjs"`
Expected: PASS toàn bộ (5 test của Task 1 + 5 test của Task 2), `# fail 0`

Nếu test safe zone thất bại, thông báo lỗi sẽ in ra pixel xa nhất và khoảng cách thực tế. Cách xử lý: giảm `stroke-width` của khiên, hoặc thu nhỏ path theo đúng bảng ở §3.4 của spec — **không** nới `SAFE_RADIUS` trong test.

- [ ] **Step 8: Commit**

```bash
git add assets/branding/*.svg assets/branding/render.mjs assets/branding/render.test.mjs assets/branding/generated .gitignore
git commit -m "feat: add AegisNet icon SVG sources and Chrome render pipeline"
```

---

### Task 3: Trang xem thử và checkpoint duyệt

**Files:**
- Create: `assets/branding/preview.html`

**Interfaces:**
- Consumes: các PNG trong `assets/branding/generated/` (Task 2).
- Produces: không có artifact cho task sau. Đây là cổng duyệt của con người.

- [ ] **Step 1: Viết `preview.html`**

Tạo `assets/branding/preview.html`:

```html
<!doctype html>
<meta charset="utf-8">
<title>AegisNet — xem thử icon</title>
<style>
  body { margin: 0; padding: 32px; background: #1a1a1a; color: #eee;
         font: 14px/1.5 system-ui, sans-serif; }
  h2 { font-size: 15px; font-weight: 600; margin: 32px 0 12px; color: #9ad; }
  .row { display: flex; gap: 24px; align-items: flex-end; flex-wrap: wrap; }
  .cell { text-align: center; }
  .cell span { display: block; margin-top: 6px; font-size: 11px; color: #888; }
  img { display: block; image-rendering: auto; }
  .circle img { border-radius: 50%; }
  .squircle img { border-radius: 28%; }
  .safe { position: relative; }
  .safe::after {
    content: ''; position: absolute; inset: 0; margin: auto;
    width: 61%; height: 61%; border: 1px dashed #ff5c5c; border-radius: 50%;
  }
  .swatches { display: flex; gap: 16px; flex-wrap: wrap; }
  .swatch { padding: 12px; border-radius: 12px; }
</style>

<h2>1 — Kích thước thật (icon đầy đủ)</h2>
<div class="row">
  <div class="cell"><img src="generated/icon_full.png" width="192"><span>192px</span></div>
  <div class="cell"><img src="generated/icon_full.png" width="96"><span>96px</span></div>
  <div class="cell"><img src="generated/icon_full.png" width="48"><span>48px — bài kiểm tra thật sự</span></div>
  <div class="cell"><img src="generated/icon_full.png" width="32"><span>32px (favicon)</span></div>
</div>

<h2>2 — Mask của Android (nền + foreground chồng lên nhau)</h2>
<div class="row">
  <div class="cell circle" style="position:relative">
    <img src="generated/icon_background.png" width="192">
    <img src="generated/icon_foreground.png" width="192" style="position:absolute;top:0;left:0;border-radius:50%">
    <span>mask tròn</span>
  </div>
  <div class="cell squircle" style="position:relative">
    <img src="generated/icon_background.png" width="192">
    <img src="generated/icon_foreground.png" width="192" style="position:absolute;top:0;left:0;border-radius:28%">
    <span>mask squircle</span>
  </div>
</div>

<h2>3 — Safe zone (vòng đỏ đứt = vùng Android đảm bảo hiển thị)</h2>
<div class="row">
  <div class="cell safe"><img src="generated/icon_foreground.png" width="256"><span>foreground — chỉ glow được tràn ra ngoài</span></div>
  <div class="cell safe"><img src="generated/icon_monochrome.png" width="256" style="background:#333"><span>monochrome — phải nằm trọn bên trong</span></div>
</div>

<h2>4 — Themed icon trên các màu wallpaper khác nhau</h2>
<div class="swatches">
  <div class="swatch" style="background:#3b5f43"><img src="generated/icon_monochrome.png" width="96"></div>
  <div class="swatch" style="background:#5c4a7d"><img src="generated/icon_monochrome.png" width="96"></div>
  <div class="swatch" style="background:#7d4a4a"><img src="generated/icon_monochrome.png" width="96"></div>
  <div class="swatch" style="background:#2b2b2b"><img src="generated/icon_monochrome.png" width="96"></div>
</div>
```

- [ ] **Step 2: Mở trang cho người dùng duyệt**

Run: `start assets/branding/preview.html` (PowerShell: `Invoke-Item assets\branding\preview.html`)

- [ ] **Step 3: DỪNG — chờ người dùng duyệt**

Trình bày trang cho người dùng và hỏi rõ:

> "Xem thử ở đây. Câu quan trọng nhất: **ở 48px bạn còn đọc ra hình khiên không?**
> Duyệt thì mình chạy fan-out, còn muốn chỉnh gì thì mình sửa SVG rồi render lại."

**Không được chạy Task 4 khi người dùng chưa duyệt.** Fan-out ghi đè hàng chục file; sửa design sau khi fan-out tốn công gấp nhiều lần.

Nếu người dùng yêu cầu chỉnh: sửa file SVG tương ứng, chạy lại `node assets/branding/render.mjs`, chạy lại `node --test "assets/branding/*.test.mjs"`, mở lại preview. Lặp đến khi được duyệt.

- [ ] **Step 4: Commit**

```bash
git add assets/branding/preview.html
git commit -m "feat: add icon preview page for design review"
```

---

### Task 4: Fan-out ra Android, iOS và web

**Files:**
- Modify: `pubspec.yaml` (thêm `flutter_launcher_icons` vào `dev_dependencies` + khối cấu hình top-level)
- Create: `assets/branding/verify.mjs`
- Generated (không viết tay): `android/app/src/main/res/mipmap-*/`, `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`, `ios/Runner/Assets.xcassets/AppIcon.appiconset/*`, `web/favicon.png`, `web/icons/*`

**Interfaces:**
- Consumes: `assets/branding/generated/*.png` (Task 2); `pngMeta`, `decodeRgba`, `hasAnyTransparent` từ `./pnginfo.mjs` (Task 1).
- Produces: `verify.mjs` chạy được độc lập, exit code 0 khi mọi kiểm tra đạt, 1 khi có mục sai.

- [ ] **Step 1: Viết `verify.mjs` (đây là test cho task này)**

Tạo `assets/branding/verify.mjs`:

```js
// Kiem tra asset da fan-out ra tung nen tang.
// Chay: node assets/branding/verify.mjs
import { existsSync, readFileSync } from 'node:fs';
import { join, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

import { pngMeta, decodeRgba, hasAnyTransparent } from './pnginfo.mjs';

const ROOT = join(dirname(fileURLToPath(import.meta.url)), '..', '..');
const failures = [];

function check(label, fn) {
  try {
    fn();
    console.log(`  OK    ${label}`);
  } catch (err) {
    failures.push(`${label}: ${err.message}`);
    console.log(`  FAIL  ${label} -- ${err.message}`);
  }
}

function assert(cond, msg) {
  if (!cond) throw new Error(msg);
}

console.log('Android:');

const MIPMAPS = { mdpi: 48, hdpi: 72, xhdpi: 96, xxhdpi: 144, xxxhdpi: 192 };
for (const [dpi, size] of Object.entries(MIPMAPS)) {
  check(`mipmap-${dpi}/ic_launcher.png la ${size}x${size}`, () => {
    const p = join(ROOT, 'android/app/src/main/res', `mipmap-${dpi}`, 'ic_launcher.png');
    assert(existsSync(p), 'khong ton tai');
    const m = pngMeta(p);
    assert(m.width === size && m.height === size, `gap ${m.width}x${m.height}`);
  });
}

check('mipmap-anydpi-v26/ic_launcher.xml khai bao du 3 layer', () => {
  const p = join(ROOT, 'android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml');
  assert(existsSync(p), 'khong ton tai — Android se im lang tut ve icon legacy');
  const xml = readFileSync(p, 'utf8');
  for (const layer of ['background', 'foreground', 'monochrome']) {
    assert(xml.includes(`<${layer}`), `thieu the <${layer}>`);
  }
});

console.log('iOS:');

const ICONSET = join(ROOT, 'ios/Runner/Assets.xcassets/AppIcon.appiconset');

check('moi file khai bao trong Contents.json deu ton tai', () => {
  const contents = JSON.parse(readFileSync(join(ICONSET, 'Contents.json'), 'utf8'));
  const names = contents.images.map((i) => i.filename).filter(Boolean);
  assert(names.length > 0, 'Contents.json khong khai bao file nao');
  const missing = names.filter((n) => !existsSync(join(ICONSET, n)));
  assert(missing.length === 0, `thieu ${missing.join(', ')}`);
});

check('icon 1024x1024 khong co kenh alpha trong suot', () => {
  const p = join(ICONSET, 'Icon-App-1024x1024@1x.png');
  assert(existsSync(p), 'khong ton tai');
  const m = pngMeta(p);
  assert(m.width === 1024 && m.height === 1024, `gap ${m.width}x${m.height}`);
  assert(
    !hasAnyTransparent(decodeRgba(p)),
    'con pixel trong suot — App Store Connect se tu choi binary',
  );
});

console.log('Web:');

for (const rel of ['web/favicon.png', 'web/icons/Icon-192.png', 'web/icons/Icon-512.png']) {
  check(`${rel} ton tai`, () => {
    assert(existsSync(join(ROOT, rel)), 'khong ton tai');
  });
}

console.log('');
if (failures.length > 0) {
  console.error(`${failures.length} muc that bai.`);
  process.exit(1);
}
console.log('Tat ca kiem tra deu dat.');
```

- [ ] **Step 2: Chạy verify để xác nhận nó thất bại**

Run: `node assets/branding/verify.mjs`
Expected: FAIL — `mipmap-anydpi-v26/ic_launcher.xml ... khong ton tai`, exit code 1. Các mipmap `ic_launcher.png` hiện có sẽ pass phần kích thước (Flutter mặc định cũng dùng đúng bộ dpi đó) — điều đó bình thường, mục XML và mục alpha iOS mới là mục đang sai.

- [ ] **Step 3: Thêm cấu hình vào `pubspec.yaml`**

Thêm vào `dev_dependencies` (ngay sau `flutter_lints: ^4.0.0`):

```yaml
  flutter_launcher_icons: ^0.14.4
```

Thêm khối top-level vào cuối file, ngang cấp với `flutter:` (không thụt lề):

```yaml
flutter_launcher_icons:
  image_path: assets/branding/generated/icon_full.png
  android: true
  adaptive_icon_foreground: assets/branding/generated/icon_foreground.png
  adaptive_icon_background: assets/branding/generated/icon_background.png
  adaptive_icon_monochrome: assets/branding/generated/icon_monochrome.png
  ios: true
  remove_alpha_ios: true
  web:
    generate: true
    background_color: "#0A0F14"
    theme_color: "#18FFFF"
```

- [ ] **Step 4: Cài dependency và chạy fan-out**

Run: `flutter pub get`
Expected: `Got dependencies!`

Run: `dart run flutter_launcher_icons`
Expected: log `✓ Successfully generated launcher icons`, không có dòng `✗`.

- [ ] **Step 5: Chạy verify để xác nhận nó pass**

Run: `node assets/branding/verify.mjs`
Expected: mọi dòng `OK`, kết thúc bằng `Tat ca kiem tra deu dat.`, exit code 0.

- [ ] **Step 6: Xem lại đúng những file nào bị thay đổi**

Run: `git status --short`
Expected: thay đổi chỉ nằm trong `android/app/src/main/res/`, `ios/Runner/Assets.xcassets/AppIcon.appiconset/`, `web/`, `pubspec.yaml`, `pubspec.lock`, `assets/branding/verify.mjs`.

Nếu có file ngoài các đường dẫn trên bị đụng, dừng lại và báo cho người dùng trước khi commit.

- [ ] **Step 7: Commit**

```bash
git add pubspec.yaml pubspec.lock assets/branding/verify.mjs android ios web
git commit -m "feat: replace default Flutter launcher icon with AegisNet icon"
```

---

### Task 5: Dọn branding còn sót và kiểm chứng build

**Files:**
- Modify: `web/manifest.json`
- Modify: `web/index.html` (chỉ nếu Step 2 phát hiện tham chiếu sai)

**Interfaces:**
- Consumes: asset đã fan-out (Task 4).
- Produces: không có (task cuối).

- [ ] **Step 1: Sửa branding trong `web/manifest.json`**

Sửa các trường sau, giữ nguyên mảng `icons` mà `flutter_launcher_icons` đã ghi:

```json
{
    "name": "AegisNet",
    "short_name": "AegisNet",
    "description": "System-wide DNS privacy guard and ad blocker powered by a Rust core engine.",
    "background_color": "#0A0F14",
    "theme_color": "#18FFFF"
}
```

Giá trị đang sai cần thay: `"name": "aegis_net"`, `"short_name": "aegis_net"`, `"description": "A new Flutter project."`, `"background_color": "#0175C2"`, `"theme_color": "#0175C2"`.

- [ ] **Step 2: Kiểm tra `web/index.html`**

Run: `grep -n "icon\|manifest\|apple-touch\|theme-color" web/index.html`

Xác nhận `<link rel="apple-touch-icon">` và `<link rel="icon">` trỏ tới file có thật (`icons/Icon-192.png` và `favicon.png` — cả hai đã được Task 4 ghi đè, nên nội dung đã đúng). Nếu có thẻ `<meta name="theme-color">` còn màu `#0175C2`, sửa thành `#18FFFF`. Nếu không có thẻ nào sai, không sửa gì ở bước này.

- [ ] **Step 3: Chạy lại toàn bộ kiểm tra tự động**

Run: `node --test "assets/branding/*.test.mjs"`
Expected: PASS, `# fail 0`

Run: `node assets/branding/verify.mjs`
Expected: `Tat ca kiem tra deu dat.`

Run: `flutter analyze`
Expected: `No issues found!` — bước này bắt lỗi cú pháp trong `pubspec.yaml`.

- [ ] **Step 4: Build APK để kiểm chứng XML adaptive icon**

Run: `flutter build apk --debug`
Expected: `√ Built build\app\outputs\flutter-apk\app-debug.apk`

Đây là chỗ duy nhất chứng minh `mipmap-anydpi-v26/ic_launcher.xml` hợp lệ — `aapt` sẽ fail nếu tham chiếu layer sai. Nếu build fail với lỗi resource, đọc kỹ thông báo của `aapt` trước khi sửa bất cứ thứ gì.

- [ ] **Step 5: Commit**

```bash
git add web/manifest.json web/index.html
git commit -m "fix: replace leftover Flutter default branding in web manifest"
```

- [ ] **Step 6: Báo cáo trung thực cho người dùng**

Báo cáo phải nêu rõ:
- Những lệnh nào đã chạy và kết quả thật của chúng.
- **Phần chưa kiểm chứng:** icon chưa được nhìn thấy trên home screen của thiết bị thật. iOS hoàn toàn chưa build được (Windows không build iOS).
- Đề xuất bước xác nhận cuối cho người dùng: cắm thiết bị Android rồi chạy `flutter install`, sau đó xem icon trên home screen — cả icon thường lẫn themed icon (bật "Themed icons" trong phần cài đặt wallpaper của Android 13+).

Không được tuyên bố "đã xong" cho phần iOS hay phần thiết bị thật.

---

## Ghi chú vận hành

**Sửa lại thiết kế về sau:** sửa file `.svg`, chạy `node assets/branding/render.mjs`, chạy `node --test "assets/branding/*.test.mjs"`, rồi `dart run flutter_launcher_icons` và `node assets/branding/verify.mjs`. Không bao giờ sửa tay PNG trong `generated/` hay trong thư mục nền tảng.

**BẮT BUỘC sau mỗi lần chạy `dart run flutter_launcher_icons`:**

```bash
git checkout -- ios/Runner.xcodeproj/project.pbxproj
```

Package 0.14.4 có bug: nó định đảm bảo `ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon` (vốn đã có sẵn trong file), nhưng bộ so khớp của nó bắt nhầm sang `ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS` và gán cho biến boolean đó giá trị `AppIcon`. Icon không cần `project.pbxproj` thay đổi gì cả — asset catalog là đủ. Không revert thì file `.pbxproj` mang một build setting sai kiểu, và Windows không build iOS được nên sẽ không ai phát hiện ra cho tới lúc lên máy Mac.

**Về `adaptive_icon_foreground_inset: 0`:** mặc định package chèn inset 16% vào foreground vì đa số người dùng đưa ảnh tràn viền. SVG ở đây đã tự tôn trọng safe zone rồi, để mặc định thì hình bị thu nhỏ hai lần (0.93 × 0.68 ≈ 63% kích thước mong muốn). Đừng bỏ dòng đó.

**Nếu `flutter pub get` không lấy được package** (mất mạng): dừng lại, báo người dùng, và chuyển sang phương án dự phòng ở §7 của spec — Chrome render thẳng từng kích thước, `Contents.json` / XML / `manifest.json` viết tay. Task 1–3 giữ nguyên giá trị, không phí công.

**Ràng buộc safe zone là bất khả xâm phạm.** Nếu test safe zone thất bại, sửa hình, không sửa hằng số trong test.
